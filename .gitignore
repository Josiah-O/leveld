node_modules/
dist/
.vercel/

# Local env (never commit secrets)
.env.local
.env.*.local

# (accidentally created) non-dot env file
env.local

# Local dev convenience server (not needed on Vercel)
scripts/local-api.mjs
